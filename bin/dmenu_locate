#!/bin/bash

. ~/.i3/dmenurc

input="$(xsel -o | $DMENU -p "file search":)"
if [ "$input" != '' ]
then
    result="$(echo "$input" | locate -e -r "$input" | $DMENU -l 5 -p "search result:" )"
    xdg-open "$result"
fi
